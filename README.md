# Restaurant Recommendation Baseline

## 1. Setup

```bash
git clone <this-repo>
cd food-reco

python -m venv .venv
source .venv/bin/activate  # (Windows: .venv\Scripts\activate)

pip install -r requirements.txt